# Malformed configuration file for testing error handling
server {
    listen 8080;
    server_name test.local;
    root ./www;
    # Missing closing brace for server block

location / {
    allow_methods GET POST;
    root ./www
    # Missing semicolon on root directive
}

# Server block without opening brace
server
    listen 8081;
    server_name broken.local;
}

# Location block with unbalanced braces
server {
    listen 8082;
    location /test {
        allow_methods GET;
        # Missing closing brace
}
